server:
  port: 8081

jwt:
  secret-key: myVeryLongSecretKeyThatIsAtLeast32Characters!
  expiration: 86400

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/corso
    username: postgres
    password: 1234567890
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
